---
description: Canonical workspace location and the no-re-root rule for all agent sessions
alwaysApply: true
---

# Workspace location — work from C:\dev, never re-root

The code repo lives **off Dropbox** (Dropbox sync caused computer freezes).

## Do
- Work only from the local repo using **full absolute paths**:
  - This machine: `C:\dev\CCH-Platform-Deploy\cch-deploy`
  - Lenovo: `C:\dev\CCH`
- `git pull` / `git fetch` are fine. **Do not commit, push, or deploy** — see `commit-deploy-policy.mdc` (only repo owner #1 does that).
- One machine per branch at a time. Repo: `github.com/CCHDESIGNSTUDIO/CCH`, branch `wip/preserve-rh-inspiration-board-2026-04-19`.

## Do NOT
- **Do NOT run `move_agent_to_root`** (or any re-root / workspace-root switch). It is unnecessary — just use full `C:\dev` paths. Sessions have wasted time on this; skip it.
- **Do NOT edit the Dropbox copy** at `...\Dropbox\CCH-Platform-Deploy\`. It is abandoned. All edits, scripts, and deploys target the `C:\dev` path only.
